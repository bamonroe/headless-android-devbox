# TODO — active task list for this project.
#
# Single source of truth for active work (see CLAUDE.md -> documentation map).
# Edit through the todo skill so ids, ordering, and metadata stay consistent:
#   scripts/todo.sh <command>      (see .claude/skills/todo/SKILL.md)
# Hand-editing works too — it is plain TOML — but the skill keeps it tidy.

schema_version = 1

[meta]
title = "TODO"
kind = "todo"
purpose = "The live task list and journal for this project — active work only."
documentation_map = "CLAUDE.md"
archive = "FINISHED.toml"

[meta.rules]
add_proposed_work = "Add proposed work as a new task rather than losing it."
remove_dropped_items = "Remove dropped items with a one-line why."
finish_work = "When a task is fully finished (built, tested, documented), run `scripts/todo.sh done <id>` to move it to FINISHED.toml."
scope = "In bounds: the emulator scaffold, the disposable APK builder, the store-publish pipeline, and their docs. Out of bounds: the app projects themselves and the BAM Store repo — track their work in their own repos."

[[task]]
id = "merge-bam-store-into-data-android-as-the-single-android-home"
title = "Merge BAM Store into /data/android as the single Android home"
description = "Merge the BAM Store project (/data/bam-store) into this repo so /data/android becomes everything Android: the APK repository, the build tools (build.sh + Dockerfile.builder), and the headless emulator. Beyond a straight merge, the APK repo should become an F-Droid-style repo, which means bringing in an F-Droid repo container (index generation/signing, fdroidserver, serving the repo) alongside the existing store publisher. Scope out the migration path (repo layout, history, config.yaml keys, publish pipeline, docs) as child scope/task items before implementing."
status = "active"
level = "epic"
category = "feature"
urgency = "normal"
order = 40
created = 2026-08-05
tags = ["merge", "bam-store", "fdroid", "epic"]

[[task]]
id = "delete-data-bam-store-retired-after-a-clean-publish-cycle"
title = "Delete /data/bam-store.retired after a clean publish cycle"
description = "The old checkout is renamed to /data/bam-store.retired and nothing references it (Caddy now serves /data/android/store/repo, bind-mounted into the caddy container). Run one publish through ./build.sh <project> from the merged home, confirm the new APK appears at http://apps.bam/index.json and downloads, then 'rm -rf /data/bam-store.retired' to reclaim ~327M of hardlinked payload."
status = "active"
level = "task"
category = "chore"
urgency = "normal"
order = 100
created = 2026-08-05
tags = []

[[task]]
id = "build-an-fdroidserver-container-image"
title = "Build an fdroidserver container image"
description = "Add Dockerfile.fdroid (tag fdroid:local) with fdroidserver + Android build-tools/aapt2 and a docker-compose service alongside the emulator, per this repo's Docker-first convention. It must be able to run 'fdroid init', 'fdroid update' and 'fdroid deploy' against a repo directory bind-mounted from the host, running as the invoking uid so generated files aren't root-owned. Add a thin scripts/fdroid.sh wrapper so nothing load-bearing lives in shell history."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 110
created = 2026-08-05
tags = []

[[task]]
id = "create-and-secure-the-f-droid-repo-signing-keystore"
title = "Create and secure the F-Droid repo signing keystore"
description = "Generate the repo signing keystore used by 'fdroid update' once, store it outside git (under directories.storage from config.yaml, e.g. <storage>/android/fdroid/keystore), and add a config.yaml key (directories.fdroid-keystore or similar) plus config.example.yaml documentation. Ensure .gitignore covers any keystore/config.py/keypass files that fdroidserver writes into the repo dir. Document the one-time setup in README.md."
status = "active"
level = "task"
category = "chore"
urgency = "normal"
order = 120
created = 2026-08-05
tags = []

[[task]]
id = "lay-out-and-initialize-the-f-droid-repo-directory"
title = "Lay out and initialize the F-Droid repo directory"
description = "Decide the on-disk layout for the F-Droid repo under store/ (e.g. store/fdroid/{repo,metadata,unsigned}) versus the existing store/repo with index.json, run 'fdroid init' through the container, and populate repo/ from the existing store/repo/apks APKs. Decide what is committed vs git-ignored (APKs and generated index files are build artifacts). Result: a working repo whose index-v1.json/index-v2.json validate and are signed."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 130
created = 2026-08-05
tags = []

[[task]]
id = "generate-per-package-f-droid-metadata-from-the-existing-store-data"
title = "Generate per-package F-Droid metadata from the existing store data"
description = "Write the metadata/<package>.yml files F-Droid needs (Name, Summary, Description, License, Categories, AuthorName) for the packages already in store/repo/index.json, and port the existing changelog sidecars (apks/<pkg>-<code>.json) into F-Droid's per-version changelog layout (metadata/<pkg>/en-US/changelogs/<versionCode>.txt). Include icon and screenshot handling: storelib currently extracts icons via aapt2 and several apps have icon=null, so decide whether icons come from the APK or from metadata/<pkg>/en-US/icon.png."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 140
created = 2026-08-05
tags = []

[[task]]
id = "run-f-droid-index-generation-alongside-the-existing-publisher"
title = "Run F-Droid index generation alongside the existing publisher"
description = "Extend the publish pipeline so a published APK lands in both the custom index.json store and the F-Droid repo during the transition: after tools/publish succeeds, copy the APK into the fdroid repo dir and run 'fdroid update' in the container. Keep BAM_STORE_PUBLISH=0 honoured and add a matching opt-out for the fdroid step. Both indexes stay in sync until the custom index.json is retired."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 150
created = 2026-08-05
tags = []

[[task]]
id = "serve-the-f-droid-repo-over-https-via-caddy"
title = "Serve the F-Droid repo over HTTPS via Caddy"
description = "Expose the generated F-Droid repo (repo/ with index-v1.json, index-v2.json, and the APKs) at a stable URL so the F-Droid client can add it as a repository. Use the /data/caddyedit API to edit the Caddyfile without root and bind-mount the new root into the caddy container (see the editing-the-caddyfile-without-root memory). Update store/repo/Caddyfile.example and README.md with the resulting config, and verify by adding the repo in an F-Droid client."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 160
created = 2026-08-05
tags = []

[[task]]
id = "decide-the-fate-of-the-com-bam-store-client-under-f-droid"
title = "Decide the fate of the com.bam.store client under F-Droid"
description = "Investigate store/client (com.bam.store) to determine what it costs to point it at the signed F-Droid index-v2.json instead of the custom index.json, and weigh that against retiring it and using the standard F-Droid client. Look at how the client parses the index, handles installs, and whether it relies on fields F-Droid doesn't provide. Produce concrete tasks for whichever path is chosen; if the client is retired, one of them is removing it from this repo and unpublishing it from the store."
status = "active"
level = "scope"
category = "feature"
urgency = "normal"
order = 170
created = 2026-08-05
tags = []

[[task]]
id = "add-f-droid-config-keys-to-config-yaml-and-the-template"
title = "Add F-Droid config keys to config.yaml and the template"
description = "The F-Droid repo work needs machine-specific paths that don't exist yet: directories.fdroid-repo (the fdroidserver repo dir on the big disk), directories.fdroid-keystore (the signing keystore, kept off git), and the public repo URL. Add them to config.yaml and config.example.yaml with comments, and note them in the CLAUDE.md storage layout table. Do this before the keystore/init tasks so they can read the paths from config.sh."
status = "active"
level = "task"
category = "chore"
urgency = "normal"
order = 180
created = 2026-08-05
tags = []

[[task]]
id = "point-build-sh-at-the-in-repo-store-publisher"
title = "Point build.sh at the in-repo store publisher"
description = "build.sh resolves the publisher via directories.bam-store in config.yaml, which now just points back at /data/android/store. Default the publisher path to $SCRIPT_DIR/store/tools/publish and treat the config key as an optional override (or drop it), so a fresh clone publishes with no config.yaml edit."
status = "active"
level = "task"
category = "refactor"
urgency = "normal"
order = 190
created = 2026-08-05
tags = []

[[task]]
id = "make-aapt2-metadata-extraction-container-based"
title = "Make aapt2 metadata extraction container-based"
description = "storelib.find_aapt2() globs SDK_DIR/build-tools on the host, so publishing depends on a host Android SDK that CLAUDE.md says should not exist. Run aapt2 dump badging inside the android-builder:local image (which already has build-tools) and fall back to a host aapt2 only if present."
status = "active"
level = "task"
category = "refactor"
urgency = "normal"
order = 200
created = 2026-08-05
tags = []

[[task]]
id = "make-publish-a-thin-wrapper-over-both-indexers"
title = "Make publish a thin wrapper over both indexers"
description = "After the fdroid container lands, tools/publish should place the APK once and then regenerate both outputs: the existing repo/index.json (for the com.bam.store client) and the F-Droid signed index. Decide ordering/failure behaviour when the fdroid container is unavailable, and keep reindex consistent with it."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 210
created = 2026-08-05
tags = []

[[task]]
id = "settle-and-document-the-publish-env-knobs"
title = "Settle and document the publish env knobs"
description = "BAM_STORE_PUBLISH, BAM_STORE_CHANGELOG and BAM_STORE_APKS are read in build.sh and storelib.py. Now that the store is in-repo, confirm the names, give them one authoritative documented home (README plus build.sh header), and make sure BAM_STORE_APKS still routes binaries to directories.bam-store-apks on the big disk."
status = "active"
level = "task"
category = "docs"
urgency = "normal"
order = 220
created = 2026-08-05
tags = []

[[task]]
id = "add-the-store-spoke-to-the-claude-md-documentation-map"
title = "Add the store spoke to the CLAUDE.md documentation map"
description = "The hub-and-spoke table in CLAUDE.md has no row for the store. Add store/CLAUDE.md as the authoritative spoke for the APK repository (contract, committed-vs-generated, client, serving), and trim root CLAUDE.md's 'Publishing to the app store' section down to a pointer plus the build.sh integration facts it genuinely owns."
status = "active"
level = "task"
category = "docs"
urgency = "normal"
order = 230
created = 2026-08-05
tags = []

[[task]]
id = "rewrite-store-claude-md-as-an-in-repo-spoke"
title = "Rewrite store/CLAUDE.md as an in-repo spoke"
description = "store/CLAUDE.md still reads as a standalone repo root: its own 'What this is', toolchain and device-install sections duplicate the root CLAUDE.md and the android-dev skill. Keep the repository contract, committed-vs-generated, and serving sections it uniquely owns; replace the duplicated toolchain/install/build prose with links to the owning docs."
status = "active"
level = "task"
category = "docs"
urgency = "normal"
order = 240
created = 2026-08-05
tags = []

[[task]]
id = "fold-store-readme-md-into-the-root-readme"
title = "Fold store/README.md into the root README"
description = "README.md documents emulator, build, install and storage but never mentions the store; store/README.md separately covers layout, publishing, serving and building the client. Give the root README a publishing section covering the automatic build.sh publish and the manual tools/publish command, and reduce store/README.md to the store-local details or delete it."
status = "active"
level = "task"
category = "docs"
urgency = "normal"
order = 250
created = 2026-08-05
tags = []

[[task]]
id = "update-the-android-dev-skill-for-the-merged-store"
title = "Update the android-dev skill for the merged store"
description = "The skill's publishing section resolves the store via config.sh get directories bam-store and points at 'that repo's own docs'. Point it at the in-repo /data/android/store instead, note that build.sh now publishes automatically, and leave a placeholder for the fdroid container commands once those tasks land."
status = "active"
level = "task"
category = "docs"
urgency = "normal"
order = 260
created = 2026-08-05
tags = []
